export const DEFAULT_TRACE_FLAG_DEBUG_LEVEL_NAME = 'LogStream_Debug';

// Default for the user-configurable trace flag duration (Settings >
// Preferences) - see appPreferences.ts's `polling` section.
export const DEFAULT_TRACE_FLAG_DURATION_MS = 30 * 60_000;
